# Copy to .env.local for local development.
# Points the admin panel at the API. In production this is baked in at build
# time (see deploy/hostinger/README.md) — e.g. https://api.yourdomain.com/api/v1/admin
VITE_API_BASE_URL=http://localhost:4000/api/v1/admin
